# Master Form Helper Text Cleanup

Updated the following accounting master-data interfaces:

- Voucher Numbering
  - Removed the transaction-category helper sentence.
  - Removed the inactive-sequence helper sentence.
  - Moved the prefix format guidance into the Prefix input placeholder.
- Party Types
  - Removed the page description.
  - Moved the internal-value format guidance into the input placeholder.
- Money Account Types
  - Removed the page description.
  - Moved the internal-value format guidance into the input placeholder.

No validation rules, controller behavior, service operations, permissions, or database logic were changed.
