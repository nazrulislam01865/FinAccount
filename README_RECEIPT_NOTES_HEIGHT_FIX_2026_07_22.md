# Receipt/Invoice Notes Height Fix

- Downloaded PDF notes box now grows only with the actual note text.
- A one-line note uses a compact box instead of filling the remaining page.
- Browser view and print CSS use the same compact, auto-height notes area.
- Applied through the shared unified document layout used by all receipts and invoices.
